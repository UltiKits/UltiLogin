# UltiLogin — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from the real-client acceptance tooling's fixed vocabulary so no
  translation step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`,
  `server`, `human`.
- **Delegated-authorization UltiCloud rows:** a row whose
  Steps can only be exercised through an authenticated UltiCloud panel session carries the fixed
  Preconditions phrase `an authorized UltiCloud session under the maintainer's delegated
  authorization` (appended to, not replacing, the row's own preconditions) and Layer `server`.
  This module's `/panel` command is such a row: it hands the player a real UltiCloud magic-link,
  which the maintainer's delegated authorization lets the acceptance tester complete
  directly, scoped to the shared UAT server only.
- **Mailpit-sink rows (this module's own addition to the authenticated-session pattern):** a
  row whose Steps can only be exercised by actually sending an email and reading the
  resulting code from a real inbox carries the fixed Preconditions phrase `an SMTP server
  reachable from the shared server with an inbox the executor can read` and Layer `server`. The
  shared server's `config.yml` `email.*` block points at a local Mailpit sink
  (no personal mail credentials involved, nothing leaves the machine), so these rows are
  executable and observed through the running server. Every row under `## Email Binding` and
  `## Password Recovery` that causes a real verification email to be sent is such a row. A
  negative row that only needs a *pending* request to already exist (not the real received code)
  still carries this phrase and Layer, because creating that pending state itself required a real
  send — see `ultilogin.email.verify.neg-wrong-code` for the one row where this distinction
  matters.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row,
  an inventory slot — and never the words "it works".
- **Covers** back-references a GUI class excluded from the JaCoCo coverage gate; left blank when no such class
  applies.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file than the row citing it.
- **Config-per-file rule:** one checklist row per `@ConfigEntity`-annotated class, never
  one row per key. The row's ID is suffixed `-yml` (`ultilogin.config.login-yml`,
  `ultilogin.config.email-yml`), aggregating every per-key `ultilogin.config.<file-stem>.*` row
  for that file rather than citing a single one of them.
- **One message source:** every line this module shows or logs comes from its language file
  (`lang/en.json`, `lang/zh.json`), so it follows the framework's `language` setting
  (UltiKits/UltiLogin#20). The three `gui-mode.title-*` settings and the twenty-one `messages.*`
  settings of `login.yml` are written into the file in the server's language while they are still
  built-in text, and the module sends exactly what the file holds (`ultilogin.lifecycle.legacy-text-defaults`).
  Every row whose Expected quotes text therefore carries `language: en` and quotes `lang/en.json`'s text
  with its colour codes left out, and assumes those settings hold the text this build writes under
  `language: en` unless its own Preconditions set one.
- **Config text rows:** `ultilogin.config.login.materialize-fresh` and `ultilogin.config.login.materialize-switch`
  exercise `FEATURES.md`'s `ultilogin.lifecycle.legacy-text-defaults` on a fresh file and across a `language`
  switch; they are named by the config-text naming convention (`<prefix>.config.<file stem>.materialize-*`)
  rather than by that ID, and the upgrade case keeps the existing row `ultilogin.lifecycle.legacy-text-defaults`.
- This module ships `login-timeout: 60`, `security.max-login-attempts: 5`,
  `security.lockout-duration: 900`, `max-register-per-ip: 3`, `password.min-length: 6`,
  `password.max-length: 32`, `gui-mode.enabled: false` (command-mode password flow) as its
  defaults; every row below assumes these unless its own Preconditions say otherwise.

## Register

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.register.create | `language: en` in UltiTools' own `config.yml`; A fresh player UUID that has never registered on this server; `gui-mode.enabled: false` (shipped default) | Run `/register abcdefgh abcdefgh`, then immediately run `/login abcdefgh` | `messages.register-success`'s text (`messages.register-success` as this build writes it under `language: en`, `&a` + the language file's `register_success`, "Registration successful! Welcome to the server!") appears; the immediate `/login` afterward shows `messages.already-logged`'s text (`messages.already-logged` as this build writes it under `language: en`, `&e` + the language file's `already_logged`, "You are already logged in!"), proving the register call auto-logged the account in | server | |
| ultilogin.register.create.neg-already-registered | `language: en` in UltiTools' own `config.yml`; `session-enabled: false`; the same player as `ultilogin.register.create`, already registered, then quit and rejoined so `RegisterCommand#register`'s own `isLoggedIn` check (which runs BEFORE `isRegistered`) sees them as unauthenticated rather than short-circuiting to `messages.already-logged` | Run `/register newpass1 newpass1` | `messages.already-registered`'s text (`messages.already-registered` as this build writes it under `language: en`, `&c` + the language file's `already_registered`, "You are already registered! Please login.") | server | |
| ultilogin.register.create.neg-ip-limit | `language: en` in UltiTools' own `config.yml`; `max-register-per-ip: 1` (NOT the shipped default 3); one account already registered from this client's IP | Register a second, fresh player from the same client/IP | "This IP has reached the maximum registration limit!" (the language file's `ip_limit_reached`), red (UltiKits/UltiLogin#20) | server | |
| ultilogin.register.create.neg-mismatch | `language: en` in UltiTools' own `config.yml`; A fresh, unregistered player | Run `/register password1 password2` (two different values) | `messages.password-mismatch`'s text (`messages.password-mismatch` as this build writes it under `language: en`, `&c` + the language file's `password_mismatch`, "Passwords do not match!") | server | |
| ultilogin.register.create.neg-too-short | `language: en` in UltiTools' own `config.yml`; A fresh, unregistered player; `gui-mode.enabled: false` | Run `/register ab ab` (2 characters, below `password.min-length: 6`) | `messages.password-too-short`'s text (`messages.password-too-short` as this build writes it under `language: en`, `&c` + the language file's `password_too_short`, "Password too short! At least {MIN} characters required."), with `{MIN}` substituted to `6` | server | |
| ultilogin.register.help | `language: en` in UltiTools' own `config.yml` | Run bare `/register` and, separately, literal `/register help` | Both print the same two lines from the language file: "Usage: /register <password> <confirm>", then "Password length: 6-32 characters" (`help_register`, `help_password_length` with the shipped `password.min-length` and `password.max-length`) (UltiKits/UltiLogin#20) | server | |

## Login

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.login.authenticate | `language: en` in UltiTools' own `config.yml`; `session-enabled: false` (isolates this row from session auto-login); a registered player, currently not logged in, whose password is known | Run `/login <the correct password>` | `messages.login-success`'s text (`messages.login-success` as this build writes it under `language: en`, `&a` + the language file's `login_success`, "Login successful! Welcome back!") | server | |
| ultilogin.login.authenticate.neg-already-logged | `language: en` in UltiTools' own `config.yml`; A currently logged-in player | Run `/login anything` | `messages.already-logged`'s text (`messages.already-logged` as this build writes it under `language: en`, `&e` + the language file's `already_logged`, "You are already logged in!") | server | |
| ultilogin.login.authenticate.neg-not-registered | `language: en` in UltiTools' own `config.yml`; A player who has never registered | Run `/login anything` | `messages.not-registered`'s text (`messages.not-registered` as this build writes it under `language: en`, `&c` + the language file's `not_registered`, "You are not registered! Please register first.") | server | |
| ultilogin.login.authenticate.neg-wrong-password | `language: en` in UltiTools' own `config.yml`; A registered, not-logged-in player, with zero prior failed attempts this lockout window; `security.max-login-attempts: 5` (shipped default) | Run `/login wrongpassword` | `messages.attempts-remaining`'s text (`messages.attempts-remaining` as this build writes it under `language: en`, `&c` + the language file's `attempts_remaining`, "Wrong password! Remaining attempts: {COUNT}"), with `{COUNT}` substituted to `4` | server | |
| ultilogin.login.authenticate.neg-lockout | `language: en` in UltiTools' own `config.yml`; Same player/IP as `ultilogin.login.authenticate.neg-wrong-password`, immediately after that row's one wrong attempt; `security.max-login-attempts: 5` (shipped default) — run 3 MORE wrong `/login` attempts first, for 4 total, before this row's own 5th | Run `/login wrongpassword` a 5th consecutive time | `messages.account-locked`'s text (`messages.account-locked` as this build writes it under `language: en`, `&c` + the language file's `account_locked`, "Too many failed login attempts! Please try again in {TIME} seconds."), with `{TIME}` substituted to `900`; a subsequent `/login <the correct password>` attempt during the lockout window is refused with the same account-locked message rather than succeeding, its `{TIME}` then being the seconds the lockout still has left (900 or a little less, since time has passed) | server | |
| ultilogin.login.authenticate.neg-wrong-password-unlimited | `language: en` in UltiTools' own `config.yml`; the module's on-disk `plugins/UltiTools/pluginConfig/UltiLogin/lang/en.json` is this build's copy — the framework replaces a copy it extracted itself and nobody edited, but keeps one it has no record of extracting whenever that copy differs from this build's, so if in doubt delete that file before starting the server and it is extracted afresh; `security.max-login-attempts: 0` (NOT the shipped default 5; the key's own comment declares `0` as unlimited), set in `login.yml` and applied with a live `/ul reload UltiLogin` on the running server, never by restarting the server on the edited file — that reload also re-reads the module's language catalogue, and at startup the framework picks the catalogue before it extracts this build's language files, so the first start after `lang/en.json` is deleted uses a stale `lang/en.yml` or `lang/en.yaml` beside it until the next reload; before this row runs, confirm that the module's `lang/` folder holds, beside `lang/en.json`, no `en.yml` and no `en.yaml` (this build ships neither), deleting any such file with the server stopped; a registered player, currently not logged in, whose password is known; no lockout in force for that player or their IP — `ultilogin.login.authenticate.neg-lockout` leaves one for 900 s, so wait it out or restart the server, which clears the in-memory lock; any restart comes before the `login.yml` edit | Run `/login wrongpassword` six consecutive times (one more than the shipped limit of 5), then `/login <the correct password>`; afterwards set `security.max-login-attempts` back to `5` and run `/ul reload UltiLogin` | Each of the six replies, read in the player's chat, is `lang/en.json`'s `wrong_password` text as shipped in this build, `Wrong password! Please try again.`, in red (an older on-disk catalogue — an older `lang/en.json`, or a stale `lang/en.yml`/`lang/en.yaml` left in effect because the setting was applied by a restart instead of the live reload — shows the same words uncoloured; that is a precondition failing, record the row `blocked`); none of the six is `messages.account-locked`'s text (the reply that carries a seconds count); the seventh command succeeds with `messages.login-success`'s text (`messages.login-success` as this build writes it under `language: en`, `&a` + the language file's `login_success`, "Login successful! Welcome back!"), proving nothing was locked (`UltiKits/UltiLogin#23`) | server | |
| ultilogin.login.authenticate.neg-wrong-password-unrecognised-lockout-type | `language: en` in UltiTools' own `config.yml`; the module's on-disk `lang/en.json` is this build's copy, as in `ultilogin.login.authenticate.neg-wrong-password-unlimited`; `security.max-login-attempts: 2` and `security.lockout-type: NONE` (neither is the shipped default), set in `login.yml` and applied with a live `/ul reload UltiLogin`, never by a restart, as in `ultilogin.login.authenticate.neg-wrong-password-unlimited`; a registered player, currently not logged in, whose password is known; no lockout in force for that player or their IP, and no failed `/login` attempt counted against that IP — the module counts wrong passwords per IP address, not per player, so every client connecting from the same address shares one count (on a single-machine harness every client is `127.0.0.1`); the count is kept only in the running module's memory, a server restart clears it, as does any registered player's successful `/login` from that address, and neither `/ul reload UltiLogin` nor `/logadmin forcelogin` clears it; so restart the server before the `login.yml` edit and let no wrong `/login` reach the server from that address between the restart and this row's first attempt (with one attempt already counted, the first reply would be the `wrong_password` text instead of the `{COUNT}` `1` text) | Run `/login wrongpassword` three consecutive times; afterwards set `security.max-login-attempts` back to `5` and `security.lockout-type` back to `IP`, and run `/ul reload UltiLogin` | The first reply is `messages.attempts-remaining`'s text (`messages.attempts-remaining` as this build writes it under `language: en`, `&c` + the language file's `attempts_remaining`, "Wrong password! Remaining attempts: {COUNT}") with `{COUNT}` substituted to `1`; the second and third replies are each `lang/en.json`'s `wrong_password` text, `Wrong password! Please try again.`; none of the three is `messages.account-locked`'s text (the reply that carries a seconds count), because `NONE` records no lock. Whether `NONE` should lock is UltiKits/UltiLogin#37, not this row (UltiKits/UltiLogin#23) | server | |
| ultilogin.login.help | `language: en` in UltiTools' own `config.yml` | Run bare `/login` and, separately, literal `/login help` | Both print the same line from the language file, "Usage: /login <password>" (`help_login`) (UltiKits/UltiLogin#20) | server | |

## Change Password

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.changepassword.update | `language: en` in UltiTools' own `config.yml`; A logged-in registered player with a known current password | Run `/changepassword <old> newpass1 newpass1`, then immediately attempt any blocked action (e.g. move a block) | "Password changed successfully!" (the language file's `change_password_success`), green; the player is IMMEDIATELY forced back to the unauthenticated state and re-prompted (F-L1: `invalidateSession` revokes the CALLER's own session too, since the caller is online) — the following move attempt is cancelled exactly as `ultilogin.protection.movement-block` describes, and a subsequent `/login newpass1` (the NEW password) succeeds | server | |
| ultilogin.changepassword.update.neg-mismatch | `language: en` in UltiTools' own `config.yml`; A logged-in player | Run `/changepassword <old> newpass1 newpass2` (two different new values) | "The two new passwords do not match!" (the language file's `change_password_mismatch`), red; the player remains logged in (only a successful change revokes) | server | |
| ultilogin.changepassword.update.neg-not-logged-in | `language: en` in UltiTools' own `config.yml`; A player who is not currently logged in; `allowed-commands` includes `changepassword` (NOT the shipped default, which omits it — `LoginProtectionListener#onPlayerCommand` would otherwise cancel this command before it ever reaches `ChangePasswordCommand`, showing the ordinary credential prompt instead of this row's own message) | Run `/changepassword a b b` | "Please login first!" (the language file's `please_login_first`, the same line `/regs` sends for the same condition), red | server | |
| ultilogin.changepassword.update.neg-wrong-old | `language: en` in UltiTools' own `config.yml`; A logged-in player | Run `/changepassword wrongold newpass1 newpass1` | "Wrong old password!" (the language file's `change_password_wrong`), red; the player remains logged in | server | |
| ultilogin.changepassword.help | `language: en` in UltiTools' own `config.yml` | Run bare `/changepassword` and, separately, literal `/changepassword help` | Both print the same two lines from the language file: "Usage: /changepassword <old> <new> <confirm>", then "Password length: 6-32 characters" (`help_change_password`, `help_password_length`) (UltiKits/UltiLogin#20) | server | |

## Email Binding

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.email.bind | `language: en`; `email.enable: true` and the framework's SMTP block correctly configured; an SMTP server reachable from the shared server with an inbox the executor can read; a logged-in player with no verified, bound email | Run `/regs <a real email address>` | `email_bind_prompt`: "A verification code has been sent to {EMAIL}. Use /regs <code> to complete binding." (yellow, `{EMAIL}` substituted) | server | |
| ultilogin.email.bind.neg-domain-blocked | `language: en`; `email.enable: true` with a valid (not necessarily reachable) SMTP configuration present — `requestEmailBind` refuses with `email_not_enabled` before checking the address at all if the framework's email service is absent/disabled, and this row does not itself trigger a send; logged in; no verified email bound yet | Run `/regs test@mailinator.com` (a domain on the shipped `domain-blacklist`) | `email_domain_blocked`: "This email domain is not allowed! Please use a mainstream email provider." (red) | server | |
| ultilogin.email.bind.neg-invalid-format | `language: en`; `email.enable: true` with a valid (not necessarily reachable) SMTP configuration present, for the same reason as `ultilogin.email.bind.neg-domain-blocked` above; logged in | Run `/regs abc@` (contains `@`, so it takes the bind branch rather than the code-verify branch, but fails the email regex) | `email_invalid_format`: "Invalid email format!" (red) | server | |
| ultilogin.email.bind.neg-not-logged-in | `language: en`; not logged in | Run `/regs test@example.com` | `please_login_first`: "Please login first!" (red) | server | |
| ultilogin.email.verify | `language: en`; the pending bind from `ultilogin.email.bind`; an SMTP server reachable from the shared server with an inbox the executor can read, which already holds the real received code | Run `/regs <the real received code>` | `email_bind_success`: "Email bound successfully!" (green) | server | |
| ultilogin.email.verify.neg-no-pending | `language: en`; logged in; no pending bind or recovery request for this player | Run `/regs 123456` | `email_no_pending`: "No pending verification request found." (red) | server | |
| ultilogin.email.verify.neg-wrong-code | `language: en`; the pending bind from `ultilogin.email.bind` still open (a real send already happened to create it, though this row does not need to read the received code itself — only a WRONG one); `verification.max-attempts: 3` (shipped default), zero prior wrong attempts on this pending request | Run `/regs 000000` (not the real code) | `email_code_invalid` with `{COUNT}` substituted: "Invalid verification code! Remaining attempts: 2" (red) | server | |
| ultilogin.email.bind.neg-already-bound | `language: en`; the same player as `ultilogin.email.verify` (already has a verified, bound email from that row) | Run `/regs another@example.com` | `email_already_bound` with `{EMAIL}` substituted to the ALREADY-bound address: "You have already bound an email: {EMAIL}" (yellow) | server | |
| ultilogin.email.help | `language: en` | Run bare `/regs` and, separately, literal `/regs help` | Both print `help_email_bind` ("/regs <email> - Bind email address") then `help_email_verify` ("/regs <code> - Verify email") — both via `plugin.i18n(...)`, respecting `language` | server | |

## Password Recovery

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.recover.request | `language: en`; not logged in; the registered account from `ultilogin.email.verify` (has a verified, bound email); at least `verification.cooldown-seconds` (60s, shipped default) elapsed since that row's own bind request was sent — `lastSendTime` is a single map shared between the bind and recovery flows, so `requestPasswordRecovery` would otherwise refuse with `email_cooldown` instead of sending; an SMTP server reachable from the shared server with an inbox the executor can read | Run `/recover` | `recover_email_sent`: "A verification code has been sent to your bound email. Use /recover <code> <new_password> <confirm> to reset." (yellow) | server | |
| ultilogin.recover.request.neg-already-logged | `language: en`; logged in | Run `/recover` | `recover_already_logged`: "You are already logged in. Use /changepassword to change your password." (yellow) | server | |
| ultilogin.recover.request.neg-no-email | `language: en`; `email.enable: true` with a valid (not necessarily reachable) SMTP configuration present — `EmailVerificationService#requestPasswordRecovery` refuses with `email_not_enabled` before checking registration or email-binding state at all; registered, not logged in, no verified bound email | Run `/recover` | `recover_no_email`: "No email bound to your account. Please contact an admin to reset your password." (red) | server | |
| ultilogin.recover.request.neg-not-registered | `language: en`; `email.enable: true` with a valid (not necessarily reachable) SMTP configuration present, for the same reason as `ultilogin.recover.request.neg-no-email` above; a player who has never registered | Run `/recover` | `recover_not_registered`: "You are not registered. Cannot recover password." (red) | server | |
| ultilogin.recover.reset | `language: en`; the pending recovery from `ultilogin.recover.request`; an SMTP server reachable from the shared server with an inbox the executor can read, which already holds the real received code | Run `/recover <the real received code> newpass1 newpass1` | `recover_success`: "Password reset successful! You have been logged in." (green); the player is now logged in with NO intervening credential prompt (`EmailVerificationService#resetPasswordAfterRecovery` → `LoginService#resetPasswordForRecovery` suppresses it) — an immediate blocked action (e.g. moving) is NOT cancelled | server | |
| ultilogin.recover.reset.neg-wrong-code | `language: en`; the pending recovery from `ultilogin.recover.request` still open, with no wrong code entered against it yet (a real send already happened to create it; this row does not need the real received code, only a WRONG one); `verification.max-attempts: 3` (shipped default) | Run `/recover 000000 newpass1 newpass1` | `Invalid verification code! Remaining attempts: 2` — the count of attempts left, where before `UltiKits/UltiLogin#21` was fixed the reply showed the literal placeholder `{COUNT}`; the password is NOT changed | server | |
| ultilogin.recover.help | `language: en` | Run literal `/recover help` (bare `/recover` reaches `requestRecovery` instead, see `ultilogin.recover.request` above) | `help_recover` ("/recover - Request password recovery code") then `help_recover_reset` ("/recover <code> <new_password> <confirm> - Reset password") — both via `plugin.i18n(...)`, respecting `language` | server | |

## Admin

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.admin.force-login | `language: en` in UltiTools' own `config.yml`; An online, registered, not-logged-in target player; an OP or `ultilogin.admin`-holding sender | Run `/logadmin forcelogin <target>` | `messages.admin.force-login`'s text (`messages.admin.force-login` as this build writes it under `language: en`, `&a` + the language file's `admin_force_login`, "Force logged in player {PLAYER}"), with `{PLAYER}` substituted, to the admin, green; "An admin has logged you in!" (the language file's `admin_force_login_notify`) to the target, green; the target is now logged in | server | |
| ultilogin.admin.force-login.neg-already-logged | `language: en` in UltiTools' own `config.yml`; The target is already logged in | Run `/logadmin forcelogin <target>` | "Player {PLAYER} is already logged in!" with `{PLAYER}` substituted (the language file's `admin_player_already_logged`), yellow (UltiKits/UltiLogin#20) | server | |
| ultilogin.admin.force-login.neg-not-found | `language: en` in UltiTools' own `config.yml`; The target is offline | Run `/logadmin forcelogin <target>` | `messages.admin.player-not-found`'s text (`messages.admin.player-not-found` as this build writes it under `language: en`, `&c` + the language file's `admin_player_not_found`, "Player {PLAYER} not found"), with `{PLAYER}` substituted | server | |
| ultilogin.admin.help | `language: en` in UltiTools' own `config.yml` | Run bare `/logadmin` and, separately, literal `/logadmin help` | Both print the same five lines from the language file: "===== UltiLogin Admin Commands =====", "/logadmin reset <player> [password] - Reset player password", "/logadmin forcelogin <player> - Force login player", "/logadmin unregister <player> - Delete player account", "/logadmin info <player> - View player account info" (`help_admin_header`, `help_admin_reset`, `help_admin_forcelogin`, `help_admin_unregister`, `help_admin_info`) (UltiKits/UltiLogin#20) | server | |
| ultilogin.admin.info | `language: en` in UltiTools' own `config.yml`; A registered target player with a bound, verified email | Run `/logadmin info <target>` | Ten lines: "===== Player Account Info =====", then nine fields labelled "Player Name:", "UUID:", "Register IP:", "Last IP:", "Login Count:", "Email:", "Email Verified:" (value "Verified"), "Register Time:", "Last Login:", each followed by the target's value; every label comes from the language file's `info_*` keys and none of the ten lines contains Chinese (UltiKits/UltiLogin#20) | server | |
| ultilogin.admin.info.neg-not-found | `language: en` in UltiTools' own `config.yml`; The target has never registered | Run `/logadmin info <target>` | `messages.admin.account-not-found`'s text (`messages.admin.account-not-found` as this build writes it under `language: en`, `&c` + the language file's `admin_account_not_found`, "Player {PLAYER} is not registered"), with `{PLAYER}` substituted | server | |
| ultilogin.admin.reset-password | `language: en` in UltiTools' own `config.yml`; A registered target; `<newpassword>` valid under command-mode length rules | Run `/logadmin reset <target> <newpassword>` | `messages.admin.password-reset`'s text (`messages.admin.password-reset` as this build writes it under `language: en`, `&a` + the language file's `admin_password_reset`, "Reset password for player {PLAYER} to: {PASSWORD}"), green, with `{PLAYER}`/`{PASSWORD}` substituted; if the target is online, they are force-revoked and re-prompted exactly as `ultilogin.changepassword.update` describes | server | |
| ultilogin.admin.reset-password.neg-not-found | `language: en` in UltiTools' own `config.yml`; The target has never registered | Run `/logadmin reset <target> anypassword` | `messages.admin.account-not-found`'s text (`messages.admin.account-not-found` as this build writes it under `language: en`, `&c` + the language file's `admin_account_not_found`, "Player {PLAYER} is not registered") | server | |
| ultilogin.admin.reset-random | `language: en` in UltiTools' own `config.yml`; A registered target; `gui-mode.enabled: false` (shipped default) | Run `/logadmin reset <target>` | `messages.admin.password-reset`'s text (`messages.admin.password-reset` as this build writes it under `language: en`, `&a` + the language file's `admin_password_reset`, "Reset password for player {PLAYER} to: {PASSWORD}"), green, with a freshly-generated 8-character alphanumeric password reported in `{PASSWORD}`; that reported password successfully logs the target in afterward | server | |
| ultilogin.admin.unregister | `language: en` in UltiTools' own `config.yml`; A registered target | Run `/logadmin unregister <target>` | `messages.admin.unregister`'s text (`messages.admin.unregister` as this build writes it under `language: en`, `&a` + the language file's `admin_unregister`, "Deleted account for player {PLAYER}"), green, with `{PLAYER}` substituted; a subsequent `/logadmin info <target>` shows `messages.admin.account-not-found`'s text (`messages.admin.account-not-found` as this build writes it under `language: en`, `&c` + the language file's `admin_account_not_found`, "Player {PLAYER} is not registered") | server | |
| ultilogin.admin.unregister.neg-not-found | `language: en` in UltiTools' own `config.yml`; The target has never registered | Run `/logadmin unregister <target>` | `messages.admin.account-not-found`'s text (`messages.admin.account-not-found` as this build writes it under `language: en`, `&c` + the language file's `admin_account_not_found`, "Player {PLAYER} is not registered") | server | |

## Panel

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.panel.open | `language: en`; `ulticloud.enabled: true`; an authorized UltiCloud session under the maintainer's delegated authorization | Run `/panel`, then click the returned link and complete the UltiCloud login in a browser | `panel_generating` ("Generating panel link...", gray) then a clickable `panel_link_sent` line ("Click here to open the panel: {URL}", green, underlined URL); once the poll confirms completion, `panel_auth_success_owner` or `panel_auth_success_player` (depending on the authenticated account's role) and the player is now logged in | server | |
| ultilogin.panel.open.neg-disabled | `language: en`; `ulticloud.enabled: false` (shipped default) | Run `/panel` | `panel_not_enabled`: "UltiCloud integration is not enabled on this server." (red) | server | |
| ultilogin.panel.help | `language: en` in UltiTools' own `config.yml` | Run literal `/panel help` (bare `/panel` reaches `openPanel` instead, see `ultilogin.panel.open` above) | "Usage: /panel - Open the UltiCloud web panel" (the language file's `help_panel`), yellow (UltiKits/UltiLogin#20) | server | |

## Player Protection

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.protection.chat-block | Not logged in; `gui-mode.enabled: false` | Send a chat message | The message is cancelled — no other player receives it; the sender receives `messages.login-prompt` or `messages.register-prompt` (per registration state) again | server | |
| ultilogin.protection.command-block | Not logged in; attempt a command NOT on the shipped `allowed-commands` list (e.g. `/help`) | Run `/help` | The command is cancelled; the same login/register prompt as `ultilogin.protection.chat-block` is re-sent | server | |
| ultilogin.protection.command-block.neg-allowed | Not logged in | Run `/login <anything>` (a command ON the shipped `allowed-commands` list) | The command is NOT cancelled by this guard — it reaches `LoginCommand#login` normally (its own validation applies separately) | server | |
| ultilogin.protection.damage-block | Not logged in (as the potential victim); a hostile mob or another player nearby able to deal damage | Attempt to take damage while not logged in | The damage event is cancelled — health is unchanged | server | |
| ultilogin.protection.damage-block.neg-attacker | An unauthenticated player attacks an ONLINE, LOGGED-IN target | The unauthenticated player attacks the logged-in target | The damage event is cancelled — the logged-in target's health is unchanged, even though the target itself is authenticated | server | |
| ultilogin.protection.inventory-block | Not logged in; `gui-mode.enabled: false` (so no credential GUI is open); a chest within reach holding at least one item; at least two distinguishable stacks in the player's own inventory, in known slots | Both branches, not either: (1) right-click the chest; (2) open the player's own inventory, click a stack to pick it up, then drag it across two other slots. Read the player's inventory NBT (`data get entity <player> Inventory`) before and after step 2 | (1) no container window is sent to the client — the chest does not open; (2) every stack named in the before-reading is still in the same slot with the same count, and the pick-up click in step 2 is itself refused, so the cursor never becomes non-empty and the drag cannot begin. **This row therefore does not observe `onInventoryDrag`**, and is not evidence about it either way: in this state the click guard refuses the pick-up first, which is exactly why that handler is documented as defensive. `LoginProtectionEventCoverageTest` is what proves the drag handler cancels. Both branches are still required because they exercise different guards — a chest is refused when the server opens it, while the player's own inventory is already visible, so its clicks have to be refused one at a time — and an earlier run of this row exercised only branch (1) (UltiKits/UltiLogin#24) | server | |
| ultilogin.protection.inventory-block.neg-credential-gui-allowed | Not logged in; `gui-mode.enabled: true`; `LoginGUIPage` or `RegisterGUIPage` currently open (its title is therefore one of the three resolved credential-GUI titles `ultilogin.protection.inventory-block` describes: under `language: en` with the three `gui-mode.title-*` settings as this build writes them, "Enter Password" or "Set Password", which contain none of the Chinese words the check used to look for) | Click a slot inside the open credential GUI | The click is NOT cancelled by this guard — the GUI's own `Icon#onClick` handler runs normally | pixel | LoginGUIPage, RegisterGUIPage |
| ultilogin.protection.join | `gui-mode.enabled: false`; `session-enabled: false`; a registered, previously-logged-out player joining | The player joins | The join broadcast is unaffected by this module; the joining player alone receives `messages.login-prompt` (or `messages.register-prompt` if unregistered); if `blind-effect: true` (shipped default) the player receives an undiminishing blindness effect | server | |
| ultilogin.protection.join.neg-valid-session | `language: en` in UltiTools' own `config.yml`; `session-enabled: true` (shipped default); the player logged in from the SAME IP within the last `session-timeout` minutes, then quit (without a server restart) | The same player, from the same IP, rejoins | "Session valid, auto-login successful!" (the language file's `session_login`), green (UltiKits/UltiLogin#20); the player is immediately logged in with no prompt and no blindness effect | server | |
| ultilogin.protection.movement-block | Not logged in | Attempt to walk to a different block | The player is snapped back to their previous block position; looking around in place (no block-coordinate change) is NOT reverted | server | |
| ultilogin.protection.quit | Logged in, with an in-flight `/panel` poll active (see `ultilogin.panel.open`) | The player disconnects | The player's login/join-time/original-location state is cleared; the in-flight panel-poll task is cancelled (confirmed by the poll never firing a completion callback after this point) | server | |
| ultilogin.protection.world-interaction-block | Not logged in; exactly 5 of one stackable block in the selected hotbar slot and the off hand empty; an armor stand summoned within reach with `ShowArms:1b` and a known tag; a breakable block, a lever and a dropped item stack all within reach; a writable book and a placeable sign in the player's inventory | Run a non-whitelisted command (for example `/help`) as a before-control. Then, reading `data get entity <player> SelectedItem` and the armor stand's full entity data immediately before and after **each** step rather than once for the whole sequence: (1) break the block; (2) place a block; (3) flip the lever; (4) right-click a living entity; (5) with the block stack selected, right-click the armor stand's arm (the armor-stand equip path — a plain block is accepted only into a hand slot, which is why the stand needs `ShowArms:1b`); (6) with the block stack still selected and the off hand empty, press the off-hand swap key; (7) select the book, right-click it **at air**, and record whether the book editor opens; if it opens, type a page of text, click Sign, enter a title, and click Sign and Close; (8) select the sign, place and write it; (9) re-select the block stack and drop it; (10) have an operator drop a stack onto the player's own position. Then run the same command again as an after-control. Finally, as a true-positive control for step 7 only and **after** that after-control, log the same player in and repeat step 7's write-and-sign with the same book | Neither control command is accepted, which proves the player stayed unauthenticated throughout. Per step: (1) the target block is still present; (2) no new block appears; (3) the lever keeps its previous state; (4) nothing about the entity changes; (5) the armor stand's entity data shows no item in any armor or hand slot and `SelectedItem` still reads count 5 — in the reported defect it fell to 4; (6) the off hand is still empty and `SelectedItem` still reads the block stack; (7) the book is still `minecraft:writable_book` with count 1 and **no text component** — both the write and the signing are refused. The client-side book editor **does** open, and that is deliberately not prevented rather than a defect: the editor is opened by the client itself while it predicts the item use, the server's `Player#openItemGui` body is empty, and `paper-api` 1.21.11 carries no book-editor-open event for a plugin to cancel (contrast the sign editor, which `PlayerOpenSignEvent` lets `LoginProtectionPaperListener#onPlayerOpenSign` refuse). An open editor here is expected; **any stored page text or any `minecraft:written_book` is the failure**; (8) no sign is placed and no sign block gains text; (9) no dropped-item entity appears; (10) the stack the operator dropped is still an entity on the ground afterwards. The selected slot changes between steps by design — steps 7, 8 and 9 each require holding a different item — so it is read per step and never asserted unchanged across the sequence. Step 8 is expected to be refused earlier in the chain — the sign cannot be placed — so a pass there does not attribute which guard refused it; `LoginProtectionEventCoverageTest` is what proves `#onSignChange` itself cancels. Step 7 is different and is attributed in-run: `LoginProtectionListener#onPlayerEditBook` is the only thing that refuses that write (nothing earlier in the chain reaches it, and no other installed plugin handles `PlayerEditBookEvent`), so its step-7 control is what makes the refusal attributable — once logged in, the identical write-and-sign must **succeed**, leaving a `minecraft:written_book` carrying the title and the typed page. If that control does not succeed, the environment never delivered the edit to the server at all and step 7's refusal proves nothing: record the row `blocked`, never `pass`. A player who *does* sign a book or write a sign while unauthenticated is a failure either way, which is why both steps stay | server | |

## GUI

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.gui.login-page | `gui-mode.enabled: true`; `gui-mode.password-length: 4` (shipped default); a registered, unauthenticated player joins | Observe the opened GUI, click four number buttons matching the account's real password, do not click Confirm | The GUI title reads the `gui-mode.title-login` value (translated `&`-codes); nine numbered wool buttons and a masked password display (space-separated, `○ ○ ○ ○` progressing to `● ● ● ●`, one glyph filled per digit entered) are visible; on the 4th digit the GUI auto-submits after a short delay without a manual Confirm click, closes, and the player is logged in | pixel | LoginGUIPage |
| ultilogin.gui.login-page.neg-wrong-password | `language: en` in UltiTools' own `config.yml`; Same as `ultilogin.gui.login-page`; zero prior failed attempts this lockout window (so this attempt lands on the attempts-remaining branch, not the lockout branch) | Enter four wrong digits | The password display resets to the space-separated `○ ○ ○ ○`; the player receives `messages.attempts-remaining`'s text (`messages.attempts-remaining` as this build writes it under `language: en`, `&c` + the language file's `attempts_remaining`, "Wrong password! Remaining attempts: {COUNT}") with `{COUNT}` substituted to one less than `security.max-login-attempts` — `LoginGUIPage#attemptLogin` forwards `LoginService#login`'s result unchanged, which under the shipped limit is `messages.attempts-remaining` or, at lockout, `messages.account-locked` (with `security.max-login-attempts: 0` it would instead be the language catalogue's `wrong_password` text, exercised through `/login` by `ultilogin.login.authenticate.neg-wrong-password-unlimited`, UltiKits/UltiLogin#23); the GUI itself remains open, not closed | pixel | LoginGUIPage |
| ultilogin.gui.register-page | `language: en` in UltiTools' own `config.yml`; `gui-mode.enabled: true`; an unregistered player joins | Observe the opened GUI; enter four digits for the first phase, then the SAME four digits again for the confirmation phase | The GUI title starts at `gui-mode.title-register`, switches to `gui-mode.title-confirm` after the first phase's 4th digit auto-advances; on the matching confirmation, `messages.register-success`'s text (`messages.register-success` as this build writes it under `language: en`, `&a` + the language file's `register_success`, "Registration successful! Welcome to the server!") appears (the same success path `ultilogin.register.create` exercises) and the GUI closes | pixel | RegisterGUIPage |
| ultilogin.gui.register-page.neg-mismatch | Same as `ultilogin.gui.register-page`, except the confirmation phase's four digits DIFFER from the first phase's | Enter four digits, then four DIFFERENT digits at the confirmation phase | `messages.password-mismatch` is sent; the GUI resets to the first phase (title reverts to `gui-mode.title-register`, password display clears) rather than closing | pixel | RegisterGUIPage |

## Scheduled Tasks

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.task.timeout-check | `language: en` in UltiTools' own `config.yml`; `login-timeout: 10` (the minimum value `@Range(min = 10, max = 600)` on `LoginConfig#loginTimeout` accepts — NOT the shipped default 60, lowered for this row only to keep the wait short; a value below 10 fails config validation and would not exercise the real timeout path at all); an unauthenticated player online | Wait at least 12 seconds without logging in — `checkTimeouts` runs only once every 20 ticks (1s) and uses a strict `elapsed > timeout` comparison, so depending on scheduler phase the kick may not fire until nearly a full second past the 10-second mark; waiting only "just over 10 seconds" can false-fail a healthy server | The player is kicked with `messages.timeout-kick`'s text (`messages.timeout-kick` as this build writes it under `language: en`, `&c` + the language file's `timeout_kick`, "Login timeout! Please reconnect.") as the disconnect reason | server | |
| ultilogin.task.verification-cleanup | `verification.code-expiry-seconds: 60` (the minimum `@Range(min = 60, max = 1800)` on `EmailConfig#codeExpirySeconds` accepts — NOT the shipped default 300, lowered for this row only); a pending email bind or recovery request created via `ultilogin.email.bind` or `ultilogin.recover.request` | Read `EmailVerificationService.java` directly: `cleanupExpired()`'s own `@Scheduled(period = 1200, ...)` fires every 60 seconds regardless of `verification.code-expiry-seconds`, and removes any `pendingBinds`/`pendingRecoveries` entry whose age exceeds that key — separately, `verifyEmailBind`/`verifyRecoveryCode` ALSO discard an expired entry inline on the very call that discovers it (returning `email_code_expired`), so no live command sequence can distinguish "the scheduled sweep already removed this entry" from "my own verification attempt just removed it on this call" — both paths converge on the identical `email_no_pending` outcome for any subsequent attempt, making a live behavioral assertion of the SCHEDULED sweep specifically unobtainable through the command surface alone | The 60-second period argument and the age-based removal condition are both present in `cleanupExpired()`'s source exactly as described; this is a source-level confirmation, not a live gameplay assertion (the same reasoning UltiChat's own `ultichat.channel.cleanup-on-quit` checklist row applies to an analogous unobservable-from-the-command-surface cleanup path) | protocol | |

## Lifecycle Hooks

`ultilogin.lifecycle.legacy-text-defaults`, `ultilogin.config.login.materialize-fresh` and
`ultilogin.config.login.materialize-switch` exercise the titles and messages written into `login.yml` in the
server's language: on an upgraded file, on a fresh file, and across a `language` switch (the last two run back
to back, the second continuing from the first's state).

`/ul reload <name>` calls that module's `reloadSelf()`, which re-reads the module's config files into the
running beans and then logs the framework's own console line `Module '<name>' reloaded.`; this module's
config file on a server is `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml`. Both framework
lines quoted in `ultilogin.lifecycle.reload` are localised by the framework itself (its `config.yml` ships
`language: "zh"`), so that row carries a `language: en` precondition governing only those two lines; the
login prompt it quotes is `LoginConfig`'s own `messages.*` text, unaffected by `language`. The row is a
regression guard, not a red row: before UltiKits/UltiLogin#29 the module's reload override already called
the framework's reload first, so the configuration was re-read before this change as well.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.lifecycle.legacy-text-defaults | `language: en` in UltiTools' own `config.yml`; server stopped; `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml` as an install upgraded from an earlier version carries it: every `gui-mode.title-*` and `messages.*` value at the Chinese default every earlier version shipped (the shared server's state), except `messages.already-logged`, set to `&eYou are in already` (an operator's own value); a byte copy of the file saved outside the server with its SHA-256; `session-enabled: false`; a registered player, not logged in, whose password is known | 1. Start the server, record the SHA-256 of that `login.yml`, and read `messages.login-success`, `messages.login-prompt`, `gui-mode.title-login` and `messages.already-logged` in it. 2. As the player run `/login <the correct password>`, then `/login anything`, and read the two replies. 3. Stop the server and start it again, then compute the SHA-256 of the file again. 4. Cleanup: stop the server and restore the byte copy (SHA-256 read back) | Step 1: `messages.login-success` is now `'&aLogin successful! Welcome back!'`, `messages.login-prompt` `'&ePlease use /login <password> to login'`, `gui-mode.title-login` `'&6Enter Password'` — every shipped default rewritten to its colour code plus the English text of `lang/en.json` — while `messages.already-logged` is still `&eYou are in already`; the file was saved. Step 2: the first reply is "Login successful! Welcome back!", the second "You are in already" — exactly the file's text. Step 3: the SHA-256 equals the one recorded in step 1 (a second start with the same language writes nothing) | server | |
| ultilogin.config.login.materialize-fresh | `language: en` in UltiTools' own `config.yml`; the server is stopped; `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml` and `plugins/UltiTools/pluginConfig/UltiLogin/lang/` moved to a backup directory outside the server (SHA-256 of each recorded), so both are written fresh; a registered player, not logged in, whose password is known | 1. Start the server; then, because a freshly written file carries the shipped `session-enabled: true` (under which a player who logged in recently from the same address is logged in without a prompt), set `session-enabled: false` in that file and run `ul reload UltiLogin` from the console. 2. Read `gui-mode.title-login`, `messages.login-prompt`, `messages.login-success` and `messages.admin.player-not-found` in `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml`. 3. Have the player join and read the prompt, then run `/login <the correct password>` and read the reply. 4. From the console run `logadmin forcelogin Nobody` (no such player online) and read the reply. 5. Leave the server running and the file as written: `ultilogin.config.login.materialize-switch` continues from this state and restores both backups at its end. | Step 2: `gui-mode.title-login: '&6Enter Password'`, `messages.login-prompt: '&ePlease use /login <password> to login'`, `messages.login-success: '&aLogin successful! Welcome back!'`, `messages.admin.player-not-found: '&cPlayer {PLAYER} not found'` — English, no Chinese text anywhere among the 24 titles and messages. Step 3: the prompt is "Please use /login <password> to login" (yellow) and the reply "Login successful! Welcome back!" (green). Step 4: "Player Nobody not found" (red) — each exactly the file's text | server | |
| ultilogin.config.login.materialize-switch | `ultilogin.config.login.materialize-fresh` passed and `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml` still holds the English text it wrote, unedited, except `messages.already-logged: '&eYou are in already'` set by hand (and `session-enabled: false`, set by `ultilogin.config.login.materialize-fresh`); the server is still running with `language: en` (run this row straight after `ultilogin.config.login.materialize-fresh`, before its backups are restored); no player has the login or registration keypad open (a keypad opened under one language stops accepting clicks once its title follows the switch, until it is reopened); `plugins/UltiTools/config.yml` backed up with its SHA-256 | 1. Set `language: zh` in `plugins/UltiTools/config.yml`, save, and run bare `ul reload` from the console (not `ul reload UltiLogin`, which does not re-read `language`). 2. Read `messages.login-success` and `messages.already-logged`. 3. Set `language: en`, save, run bare `ul reload` again, and read the two keys. 4. Cleanup: stop the server; restore `plugins/UltiTools/config.yml` from its backup and `config/login.yml` and `lang/` from the backups `ultilogin.config.login.materialize-fresh` took (SHA-256 of each read back). | Step 2: `messages.login-success` is byte-identical to `"&a" + login_success` of the jar's `lang/zh.json` (the Chinese text an earlier version shipped), and `messages.already-logged` is still `&eYou are in already`. Step 3: `messages.login-success` is `'&aLogin successful! Welcome back!'` again and `messages.already-logged` is still `&eYou are in already` — an untouched value follows the switch in both directions, an edited one never does | server | |
| ultilogin.lifecycle.reload | `language: en` in `plugins/UltiTools/config.yml` (governs the two framework lines and the login prompt quoted in Expected); `UltiLogin` loaded; in `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml`, BEFORE the server starts: `login-timeout: 600` (the `@Range` maximum, so `ultilogin.task.timeout-check` cannot kick the test player while the file is edited), `session-enabled: false` (no session auto-login), `gui-mode.enabled: false` (shipped default, text prompt), and `allowed-commands` at its shipped default `[login, l, register, reg, panel, regs, recover]`, which does not contain `help`; a registered test player who has just joined, has NOT logged in, and keeps Bukkit's default `bukkit.command.help` permission; the refusal this row relies on is the one `ultilogin.protection.command-block` describes; `ul` runs from the server console; steps 1-5 complete within 600 seconds of the player joining, without the player rejoining or logging in | 1. As the test player, run `/help` and record the result. 2. On the host, add `help` as one more entry of `allowed-commands` in that `login.yml` (a new `- help` line), change nothing else, and save the file. 3. From the server console, on the SAME running server (no restart), run `ul reload UltiLogin`. 4. As the SAME still-unauthenticated player, run `/help` again. 5. Remove the `- help` line, save, run `ul reload UltiLogin` from the console again, then as the same player run `/help` once more. 6. Cleanup: restore `login-timeout`, `session-enabled` and `allowed-commands` to their values before this row and run `ul reload UltiLogin`. | Step 1: the command is cancelled — no help page appears — and the player is re-sent `messages.login-prompt`'s text (`messages.login-prompt` as this build writes it under `language: en`, `&e` + the language file's `login_prompt`, "Please use /login <password> to login"), exactly as in `ultilogin.protection.command-block`; if a help page appears instead, the precondition is wrong (the player is logged in, or `help` is already allowed): record the row `blocked`, never `pass`. Step 3: the console shows a line ending `Module 'UltiLogin' reloaded.` and the console command reply `Module UltiLogin has been reloaded`, with no reload line from the module itself; the one line the module may add is the removed-key WARNING of `ultilogin.lifecycle.removed-key-warning`, printed only while this `login.yml` still contains `messages.wrong-password` (true of any server that ran an earlier version), and it is not a reload line. Step 4: the command is NOT cancelled — the server's help page appears and no login prompt follows — so the edited list took effect without a restart. Step 5: after the second reload the command is cancelled again and the login prompt is re-sent, proving the list is re-read in both directions rather than only widened | server | |
| ultilogin.lifecycle.removed-key-warning | `language: en` in UltiTools' own `config.yml`; `UltiLogin` loaded from this build; the server console readable; `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml` contains the key `messages.wrong-password` with any value — any server that ran an earlier version has it, because the framework wrote it there and never deletes a key; if it is absent, stop the server, add a line `  wrong-password: 'x'` under that file's `messages:` block, and save | 1. Start the server (or restart it) and read the console output of UltiLogin's load. 2. From the console run `ul reload UltiLogin` and read the output. 3. Delete the `wrong-password` line from that `login.yml`, change nothing else, save, and run `ul reload UltiLogin` again. 4. Cleanup: none — the key does nothing in this version, so it stays deleted | Steps 1 and 2 each: exactly one WARNING line from `[UltiLogin]` containing the path of that `login.yml`, `still contains 'messages.wrong-password', which this version of UltiLogin no longer reads`, `wrong_password`, and `Delete the key from the file to silence this warning.` Step 3: after the key is deleted, the reload prints no such line — steps 1 and 2 are the positive control that the check can see the key, and step 3 shows the key is what drives it (UltiKits/UltiLogin#23) | server | |

## Data persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.account.restart-survival | An account registered via `ultilogin.register.create`, with its email verified via `ultilogin.email.verify`; not logged in at the time of the restart; the account's CURRENT login count noted via `/logadmin info <the account's player>` BEFORE the restart | Stop the server completely, then start it again, then rejoin and run `/login <the original password>`, then run `/logadmin info <the account's player>` again | The `/login` attempt with the pre-restart password succeeds (`messages.login-success`, proving the password hash/salt survived); `/login` itself increments `loginCount` (`LoginService#login`), so the post-restart `/logadmin info` shows the login count as EXACTLY ONE MORE than the pre-restart value noted above, not identical to it; IP history and bound/verified email read back identical to their pre-restart values | server | |
| ultilogin.session.not-persisted | `session-enabled: true` (shipped default); `session-timeout: 30` (shipped default); a player logs in from a known IP, then quits, well within the 30-minute window | Stop the server completely, then start it again, then have the same player rejoin from the same IP | The player is NOT auto-logged-in — they receive the ordinary `messages.login-prompt` exactly as a first-time-this-session join would, because the in-memory session map does not survive the restart, regardless of the 30-minute window not having elapsed | server | |

## Configuration

One row per `@ConfigEntity` class (the config-per-file rule), not per key: `login.yml`
(43 keys) and `email.yml` (8 keys) — 51 keys total, matching `FEATURES.md`'s `## Configuration`
section exactly. Each row confirms every key in the file is present at its `FEATURES.md`-documented
default, then flips one or more representative keys and observes the behaviour follow.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.config.login-yml | `language: en` in UltiTools' own `config.yml`; fresh `plugins/UltiTools/pluginConfig/UltiLogin/config/login.yml` (moved aside before the server started, so this build writes it; the titles and messages then hold the English text `FEATURES.md` quotes for each key) | Load the file; confirm all 43 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults, and that the file has no `messages.wrong-password` key (UltiKits/UltiLogin#23 removed it, so a fresh file must not carry it); then set `gui-mode.enabled: true` (default `false`), restart, and confirm a fresh player join opens `RegisterGUIPage`/`LoginGUIPage` rather than the text prompt (see `ultilogin.gui.register-page`/`ultilogin.gui.login-page`); separately, set `security.max-login-attempts: 2` (default 5) and confirm lockout now triggers on the 2nd wrong `/login` attempt, not the 5th | All 43 keys present at their documented defaults before the change, and no `messages.wrong-password` key in the fresh file; after `gui-mode.enabled: true`, the GUI opens on join instead of the text prompt; after `security.max-login-attempts: 2`, `messages.account-locked` appears on the 2nd wrong attempt rather than the 5th | server | |
| ultilogin.config.email-yml | Fresh `plugins/UltiTools/pluginConfig/UltiLogin/config/email.yml` at its shipped default; `email.enable: true` and the Mailpit sink configured as described in the conventions above | Load the file; confirm all 8 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults; then set `verification.code-length: 4` (default 6) and request a new email bind, reading the actual received code's length | All 8 keys present at their documented defaults before the change; after the change, the newly-sent code is 4 digits long, not 6 | server | |

## Language

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultilogin.i18n.language | `language: en` in UltiTools' own `config.yml`, set once for the session before the start; before the start, `plugins/UltiTools/pluginConfig/UltiLogin/lang/` was moved into the session backup directory so this jar's catalogues are extracted fresh (an upgraded install never refreshes an already-extracted language file, UltiKits/UltiTools-Reborn#459); the three `gui-mode.title-*` settings and every `messages.*` setting of `login.yml` holding the English text this build writes under `language: en` (a fresh file, or one `ultilogin.lifecycle.legacy-text-defaults` has rewritten); `gui-mode.enabled: false`; an OP test player, registered and logged in | 1. Read the console line UltiLogin printed when it was enabled. 2. As the test player run bare `/login`, bare `/register`, bare `/logadmin` and literal `/panel help`, and read the replies | Step 1: `UltiLogin has been enabled!`. Step 2: `/login` prints "Usage: /login <password>"; `/register` prints "Usage: /register <password> <confirm>" and "Password length: 6-32 characters"; `/logadmin` prints the five lines `ultilogin.admin.help` quotes; `/panel help` prints "Usage: /panel - Open the UltiCloud web panel". None of the text these show contains Chinese | server | |
